#!/bin/sh

set -e

CONFIG="$SNAP_COMMON"
if [ ! -f $CONFIG/dnsmasq.conf ]; then
  echo "not found"
  cp $SNAP/configs/* $CONFIG
fi
while :; do
    if [ -e "$CONFIG" ]; then
        $SNAP/bin/pihole-FTL
    fi
    sleep 5
done

